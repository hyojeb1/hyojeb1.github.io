# hyojeb1.github.io

장효제 포트폴리오. 바닐라 HTML + CSS, 빌드 없음.

```
index.html
style.css
assets/        profile.jpg, *.mp4, *-poster.jpg, resume.pdf
```

로컬 확인은 `index.html`을 브라우저로 열면 된다.
`main`에 푸시하면 `.github/workflows/pages.yml`이 위 세 경로만 Pages에 올린다.
