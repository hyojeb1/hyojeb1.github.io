# 장효제 포트폴리오 — 디자인 명세

구현 에이전트용. 에이전트는 **구현자**이며 디자이너가 아니다.
여기에 없는 시각적 결정이 필요해지면 임의로 채우지 말고 묻는다.

---

## 0. 목적

방문자(게임 회사 채용 담당자, 기술 면접관)가 30초 안에 다음을 알게 한다.

1. 이 사람은 게임을 실제로 완성한다.
2. 이 사람은 시스템을 설계하고, 팀의 병목을 도구로 해결한다.
3. 연락할 방법이 여기 있다.

**주연은 "문제를 구조화하는 사람"이다.** 다만 그것만 앞세우면 "코드는 짜나?"가 남으므로
최상단 첫 카드는 반드시 **돌아가는 게임 화면**이어야 한다. 이 순서는 협상 대상이 아니다.

**영상은 1번만 증명한다. 2번은 카드 본문에만 있다.** 본문을 hover 뒤로 숨기거나
지나치게 줄이면 사이트가 목적의 절반을 잃는다.

---

## 1. 스택

**바닐라 HTML + CSS.** 빌드 도구 없음. 프레임워크 없음. npm 의존성 없음.

- `index.html` + `style.css` 두 파일. 자산은 `/assets/` 한 폴더.
- JS는 인라인 `<script>` 하나. 현재 용도는 **hover 재생 하나뿐**이며,
  CSS로는 `<video>` 재생을 시작시킬 수 없어 불가피하다. 다른 용도로 늘리지 않는다.
- 배포는 `.github/workflows/pages.yml`이 세 경로만 올린다. 새 최상위 경로는 그 목록에 넣는다.

---

## 2. 페이지 구조

상세 페이지를 만들지 않는다. 프로젝트 상세는 GitHub README와 영상으로 외부화한다.

```
[1] 헤더    이름 + 내비(works / about / contact)
[2] 히어로  프로필 사진(좌) + 선언 문장(우)          #about
[3] works   대표 카드(세로로 쌓임) + 보조 카드(그리드)  #works
[4] 푸터    연락처                                   #contact
```

**섹션은 하나(`works`)로 유지한다.** 분류는 항목이 셋 이상일 때만 값을 한다.
하나뿐인 항목은 카테고리가 아니라 근거이므로, 그것을 뒷받침하는 카드 안에 둔다.

늘어날 때의 순서:

1. 보조 카드 4개 초과 → `other work` **텍스트 목록** (카드도 섹션도 아님)
2. 기사 3개 이상 → `press` 섹션
3. 대표 카드 4개 초과 → 상세 페이지를 만들 시점

### 히어로

- 좌측: 프로필 사진 **3:4**, 폭 168px, `border-radius: 4px`.
- 우측: 직군 라벨(11–12px) → **선언 문장**(21px, 2행 이내) → 보조 설명(14px, 2~3행)
  → 링크.
- **선언 문장은 직함이 아니라 문장이다.** 그리고 카드들이 실제로 증명하는 것과
  같은 축이어야 한다. 카드가 증명하지 않는 성향을 선언하면 히어로가 혼자 뜬다.
- 히어로에는 카드를 두지 않는다. 카드는 전부 `works` 안에 있다.
- 링크는 **최대 4개**. 각 링크는 "클릭하면 무엇을 알게 되는가"에 한 문장으로 답해야 한다.
  답이 "저를 더 알 수 있어요"면 넣지 않는다. 빈 채널·멈춘 블로그는 없느니만 못하다.
- 사진과 텍스트는 수직 중앙 정렬.

### 대표 카드

**한 과정의 마무리 단위.** 여러 개가 될 수 있고 최신이 위로 쌓인다.

- 전체 폭, 정사각형(`aspect-ratio: 1/1`). 미디어는 상단에 16:9.
- 테두리만 `2px solid` 액센트. 배경색은 다른 카드와 동일.
- 좌상단 `playable build` 배지 — 플레이 가능한 빌드 링크가 있을 때만 활성화.
- 제목 좌측, 메타(`팀 N인 · 기간 · 담당 역할`) 우측.
- 본문 **최대 7행**: 문제 → 고려한 선택지 → 왜 이걸 골랐나 → 결과.
  기능 나열이 아니라 판단 서술. 넘치면 말줄임으로 드러난다.
- 하단에 스택 태그, 그 아래 외부 링크(저장소·영상 등).

### 보조 카드

**형식 자유.** 대표 카드보다 작다는 것만 지킨다. 그 크기 차이가 위계다.

- 2열 그리드, 정사각형. 개수 제한 없음.
- **순서: 프로젝트가 먼저, 기사·수상 같은 근거가 나중.**
- 미디어는 영상이든 정지 이미지든 상관없다. 16:9만 지킨다.
- 본문 **최대 3행**.
- 태그·링크 개수·카드 전체 링크 여부는 카드마다 판단한다.
  링크가 둘 이상이면 `<a>` 중첩이 불가능하므로 `<article>` + 개별 링크로 간다.
- 프로젝트가 아니어도 된다(기사, 도구, 실험).

**카드당 하나만 증명한다.** 각 카드가 다른 것을 증명해야 방문자가 인물을 합성한다.

---

## 3. 타이포그래피

ONE Mobile 3종을 서브셋 WOFF2로 사용한다. 웹 임베딩 허용 확인됨.

| 용도 | 서체 | 크기 |
|---|---|---|
| 워드마크 | Title | 16px |
| 선언 문장 | Title | 21px |
| 카드 제목 | Title | 15–16px |
| 본문 | Regular | 14 / 13px |
| 메타·라벨·태그·링크·내비 | POP | 11–12px |

- **전부 굵기 400.** Title과 POP은 단일 굵기라 500 이상을 지정하면 가짜 볼드가 합성된다.
  굵기가 아니라 **서체와 크기**로 위계를 만든다.
- 본문 `line-height` 1.6–1.7.
- POP은 메타데이터 전용. 제목과 본문에 쓰지 않는다.
- 폰트는 서브셋 산출물이고 원본 OTF는 저장소에 없다. 재생성 명령은 `AGENTS.md`.

---

## 4. 색

**밝은 배경.** 게임 스크린샷이 대체로 어두워서, 어두운 배경에 올리면 경계가 사라지고
전부 회색으로 뭉개진다. 밝은 배경이 액자 역할을 한다.

- 흰 배경, 거의 검은 본문, 회색 3단계(본문 / 보조 / 힌트).
- 테두리 `0.5px`.
- 모서리 `8px`.

### 액센트

`#F1EC95`. 연한 노랑이고 **흰 배경 대비가 약 1.2:1**이라 §9의 4.5:1을 통과하지 못한다.
따라서 **글자색으로 쓰지 않는다.** 형광펜처럼 쓴다 — 글자는 어두운 색을 유지하고
액센트는 면과 선으로만 나타난다.

| 자리 | 방식 |
|---|---|
| 대표 카드 테두리 | `2px solid` |
| 외부 링크 | 밑줄 `2px`, hover 시 배경 |
| 활성 `playable build` 배지 | 배경 |
| 포커스 링 | **쓰지 않는다.** 안 보인다. 본문색을 쓴다 |

액센트를 새로 쓸 자리가 생기면 대비부터 확인한다. 글자에 닿으면 잘못된 사용이다.

---

## 5. 미디어

- 프로필: 3:4 JPEG.
- 카드 미디어: **16:9**. 영상 또는 정지 이미지.
- 영상은 **6~10초 무음 루프**, 720p 이하, **2MB 이하**,
  `<video muted loop playsinline preload="none">` + `poster` 필수.
- 풀 플레이 영상은 카드에 넣지 않는다. 텍스트 링크로 내보낸다.
- 유튜브 임베드 금지. 썸네일이 필요하면 내려받아 `/assets/`에 둔다.
- 데스크톱 자동 재생 금지.

### hover 재생

**hover는 터치 기기에 없다.** 채용 담당자는 폰으로 먼저 여는 경우가 많다.

- `(hover: hover)`: hover 시 재생, 벗어나면 일시정지.
- `(hover: none)`: 대표 카드만 자동 재생. 나머지는 poster 유지.
- `prefers-reduced-motion`이면 전부 정지.
- 미디어에 `<video>`가 없는 카드가 있으므로 스크립트는 반드시 null을 확인한다.

---

## 6. 모션

**콘텐츠의 공간 관계를 설명할 때만 쓴다.**

허용: hover 시 테두리 색 전환, 링크 밑줄, 영상 재생 전환. 150–200ms 이내.
금지: 스크롤 연동 애니메이션, parallax, 페이드인 등장, 페이지 전환 효과.

---

## 7. 반응형

- 컨테이너 최대 폭 740px. 본문 한 줄이 한글 50자를 넘지 않는 상한이다.
- 좌우 여백 최소 20px. 최소 지원 폭 320px.
- 600px 아래에서:
  - 히어로 세로 적층, 사진 폭 132px
  - 보조 카드 2열 → 1열
  - **카드 정사각형 해제.** 좁은 폭에서는 텍스트 예산이 부족해 정사각형과 본문이 양립하지 않는다.
  - 본문 말줄임 해제 — 잘린 문장은 뜻이 사라진다.

---

## 8. 금지 목록

장효제를 설명하는 데 기여하지 않는 것은 버린다.

- Three.js, WebGL, 커스텀 셰이더, 3D 씬
- 커스텀 커서, 로딩 시퀀스, 스플래시 화면
- glassmorphism, 네온 글로우, 그라디언트, 드롭섀도우
- 다크 배경, 터미널 에뮬레이터 연출, 타이핑 애니메이션
- **배경 이미지** — 어두운 게임 영상의 액자인 흰 배경을 무너뜨린다
- **유튜브 임베드** — 스크립트·쿠키가 무겁고 플레이어 UI가 톤을 깬다
- **본문을 hover 뒤로 숨기는 오버레이** — §0의 2번이 사라지고 키보드·스크린리더에서 도달이 어렵다
- **기술 스택 로고 그리드** — 정보량 0
- 스킬 레벨 게이지, 별점, 퍼센트 바
- 방문자 카운터, 애널리틱스 배지, "made with ♥"
- AI 개발자 포트폴리오 템플릿의 관습적 요소 일반

---

## 9. 접근성 최소선

- 자동 재생 시 `prefers-reduced-motion` 존중.
- 이미지 `alt` 필수. 장식 요소는 `aria-hidden`.
- 텍스트 대비 4.5:1 이상.
- 키보드 탭 순서가 시각적 순서와 일치. 포커스 링을 제거하지 않는다.

---

## 10. 콘텐츠

**문구를 창작하지 않는다.** 근거와 자리표시자 규칙은 `AGENTS.md`.

---

## 11. 완료 기준

- `index.html` + `style.css` 두 파일로 끝난다.
- 빌드 명령 없이 브라우저에서 `index.html`을 열어 확인 가능하다.
- 320px과 1440px 모두에서 레이아웃이 무너지지 않는다.
- 미디어가 전부 없어도 회색 사각형이 자리를 지키며 레이아웃이 깨지지 않는다.
- 금지 목록의 항목이 하나도 포함되지 않는다.
