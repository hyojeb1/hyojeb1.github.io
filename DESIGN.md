# 디자인 명세

`index.html`과 `style.css`가 이미 말하는 것은 여기 적지 않는다. 수치·배치·반응형은
코드를 읽는다. 여기에는 **코드를 읽어도 알 수 없는 것**만 있다.

여기에 없는 시각적 결정이 필요해지면 임의로 채우지 말고 묻는다.

---

## 목적

방문자(게임 회사 채용 담당자, 기술 면접관)가 30초 안에 다음을 알게 한다.

1. 이 사람은 게임을 실제로 완성한다.
2. 이 사람은 시스템을 설계하고, 팀의 병목을 도구로 해결한다.
3. 연락할 방법이 여기 있다.

**2번은 클릭한 사람에게만 전달된다.** 판단 서술을 상세 페이지로 내리면서
의도적으로 감수한 손실이다. 첫 화면은 목적 1번(완성한다)과 3번(연락처)만
책임진다. 나중에 "왜 시스템 설계자로 안 읽히지?"라는 의문이 들면 원인은 여기다.

---

## 카드

**카드는 목차이고, 글은 상세 페이지에 있다.** 카드에는 제목·메타·태그·미디어만
둔다. 카드가 커지면 첫 화면이 길어지고, 그게 읽기를 막는다.

- **카드 전체가 링크다.** 그래서 카드 안에 다른 링크를 두지 않는다. `<a>` 중첩은
  성립하지 않는다. 저장소·빌드·영상 링크는 전부 상세 페이지에 있다.
- **크기가 위계다.** 대표는 가로 2칸, 보조는 1칸. 높이는 둘 다 한 칸으로 같다.
  제목이나 색이 아니라 면적이 "이게 대표작"을 말한다.
- 한 칸 높이에 16:9는 들어가지 않는다. 대표 카드의 미디어는 **16:4.5로 잘라** 쓴다.
  잘려도 되는 이유는 카드가 목차이기 때문이다. 전체 화면은 상세 페이지에서 본다.
- **시간순으로 놓는다.** 대표 카드가 남기는 빈칸은 그대로 둔다.
  빈칸을 메우려고 순서를 흐트러뜨리지 않는다.
- 형식은 카드마다 판단한다. 태그·미디어 종류·프로젝트 여부 모두 자유다.
- 외부 기사처럼 상세를 쓸 게 없는 카드는 바깥으로 바로 나가도 된다.

### 상세 페이지 (`works/*.html`)

- **카드당 하나만 증명한다.** 여러 개를 증명하려 들면 기능 나열로 돌아간다.
- 본문은 **문제 → 고려한 선택지 → 왜 이걸 골랐나 → 결과**. 기능 목록이 아니라 판단 서술.
- 여기서는 자르지 않는다. 전문을 읽는 자리다.
- 새 페이지를 만들면 `.github/workflows/pages.yml`의 복사 목록을 확인한다.
  거기 없으면 로컬에서는 보이고 배포에서는 사라진다.

**섹션은 하나(`works`)로 유지한다.** 분류는 항목이 셋 이상일 때만 값을 한다.
하나뿐인 항목은 카테고리가 아니라 근거이므로 그것을 뒷받침하는 카드 안에 둔다.
내비도 섹션이 둘 이상일 때 되살린다.

---

## 밟으면 아픈 것 셋

**1. 밝은 배경이어야 한다.** 게임 스크린샷이 대체로 어두워서, 어두운 배경에 올리면
경계가 사라지고 전부 회색으로 뭉개진다. 밝은 배경이 액자 역할을 한다.

**2. 액센트 `#F1EC95`는 흰 배경에서 글자가 될 수 없다.** 대비가 약 1.2:1이다.
흰 바탕에서는 테두리·밑줄·배경으로만 쓰고 포커스 링에도 쓰지 않는다.

색 조합은 **반전 하나**로 고정한다. 섞으면 카드 위에 계열이 셋이 된다.

| 면 | 글자 |
|---|---|
| 노랑 | 검정 `--text` |
| 검정 | 노랑 `--accent` |
| 흰색 | 회색 `--text-3` (비활성 표시용) |

**3. `–`(U+2013)와 `—`(U+2014)를 쓰면 안 된다.** ONE Mobile 3종 모두 글리프가
비어 있다. cmap에는 있어서 폴백도 안 걸리고 조용히 빈칸이 된다. 원본 OTF부터 그렇다.
기간은 `~`, 제목 구분은 `·`, 본문에서는 문장을 끊는다.
새 기호를 도입할 때는 세 폰트 모두에서 글리프가 비어 있지 않은지 확인한다.

---

## 금지 목록

전부 한 번씩 검토하고 기각한 것들이다. 다시 논의하지 않기 위해 적어둔다.

| | 이유 |
|---|---|
| 다크 배경 | 위 1번 |
| 배경 이미지 | 위 1번을 무너뜨린다 |
| 유튜브 임베드 | 스크립트·쿠키가 무겁고 플레이어 UI가 톤을 깬다. 썸네일은 내려받아 쓴다 |
| 본문을 hover 뒤로 숨기는 오버레이 | 목적 2번이 사라지고 키보드·스크린리더에서 도달이 어렵다 |
| 기술 스택 로고 그리드 | 정보량 0 |
| 스킬 게이지, 별점, 퍼센트 바 | 자기 평가는 근거가 아니다 |
| Three.js, 커스텀 커서, 로딩 시퀀스, 타이핑 애니메이션 | 장효제를 설명하지 않는다 |
| 그라디언트, 네온, 글래스모피즘, 드롭섀도우 | 같음 |
| 방문자 카운터, "made with ♥", 카피라이트 | 같음 |

**모션은 콘텐츠의 공간 관계를 설명할 때만.** 스크롤 연동, parallax, 페이드인 등장은 쓰지 않는다.

---

## 접근성 하한

- 텍스트 대비 4.5:1 이상. 가장 흐린 회색이 이미 4.9:1이라 여유가 없다.
- 이미지 `alt` 필수, 장식은 `aria-hidden`.
- 포커스 링을 제거하지 않는다.
- 자동 재생에는 `prefers-reduced-motion`을 존중한다.
- hover는 터치 기기에 없다. 채용 담당자는 폰으로 먼저 여는 경우가 많다.

---

## 콘텐츠

문구를 창작하지 않는다. 근거와 자리표시자 규칙은 `AGENTS.md`.
