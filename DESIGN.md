# 장효제 포트폴리오 — 디자인 명세

이 문서는 구현 에이전트를 위한 것이다. 에이전트는 **구현자**이며, 디자이너가 아니다.
여기에 적히지 않은 시각적 결정이 필요해지면, 임의로 채우지 말고 질문한다.

---

## 0. 이 사이트의 목적

방문자(게임 회사 채용 담당자, 기술 면접관)가 30초 안에 다음을 알게 한다.

1. 이 사람은 게임을 실제로 완성한다.
2. 이 사람은 시스템을 설계하고, 팀의 병목을 도구로 해결한다.
3. 연락할 방법이 여기 있다.

**주연은 "문제를 구조화하는 사람"이다.** 다만 그것만 앞세우면 "코드는 짜나?"가 남으므로,
페이지 최상단의 첫 프로젝트는 반드시 **돌아가는 게임 화면**이어야 한다.
이 순서는 협상 대상이 아니다.

---

## 1. 스택

**바닐라 HTML + CSS.** 빌드 도구 없음. 프레임워크 없음. npm 의존성 없음.

- 페이지가 1개이고, 동적 데이터가 없고, 라우팅이 없다. 프레임워크의 이점이 발생하지 않는다.
- GitHub Pages에 `index.html`을 그대로 올려 배포한다.
- 자산은 `/assets/` 한 폴더에 둔다.
- JS는 필요한 경우에만, 인라인 `<script>` 하나로.

```
/
  index.html
  style.css
  /assets
    profile.jpg
    rop.mp4  rop-poster.jpg
    proj2.mp4  proj2-poster.jpg
    proj3.mp4  proj3-poster.jpg
    resume.pdf
```

기존 리포(`hyojeb1/hyojeb1.github.io`)의 Astro 프로젝트는 전부 제거하고 위 구조로 대체한다.
기존 블로그 글은 **이관하지 않는다.**

---

## 2. 페이지 구조 — 4블록, 1페이지

상세 페이지를 만들지 않는다. 개인 프로젝트의 상세는 GitHub README로 외부화한다.

```
[1] 헤더        이름 + 내비(work / about / contact)
[2] 히어로      프로필 사진(좌) + 선언 문장(우)
[3] 대표 작업   두꺼운 카드 1개 + 보조 카드 2개
[4] 푸터        연락처
```

Writing 섹션은 만들지 않는다.

### [2] 히어로

- 좌측: 프로필 사진, **3:4 세로**, 폭 약 168px 기준. `border-radius: 4px`.
- 우측: 위에서 아래로
  1. 직군 라벨 (monospace, 12px, 가장 흐린 회색)
  2. **선언 문장** (21px, weight 500, 2행 이내) — 직함이 아니라 문장.
  3. 보조 설명 (14px, 2~3행)
  4. 링크 3개: `résumé.pdf` / `github` / `mail` — monospace 12px, 아웃라인 버튼
- 사진과 텍스트는 수직 중앙 정렬. 사진이 텍스트 블록보다 짧거나 길어도 무너지지 않게.

### [3] 대표 작업

**의도된 비대칭.** 3개를 같은 크기로 놓지 않는다. 크기 차이가 "이게 대표작"이라는 신호다.

**대표 카드 (팀 프로젝트)**
- 전체 폭. 미디어 영역 16:9, 높이 약 150px.
- 테두리만 `2px solid` 액센트 색. 배경색은 다른 카드와 동일하게 유지한다.
- 좌상단에 `playable build` 배지 (빌드/영상 링크가 준비되면 활성화).
- 제목(16px/500) 좌측, 메타(monospace 11px, `팀 N인 · N개월 · 담당 역할`) 우측 같은 줄.
- 본문 5~6행: **문제 → 고려한 선택지 → 왜 이걸 골랐나 → 결과.** 기능 나열이 아니라 판단 서술.
- 하단에 스택 태그 (monospace 11px, 연한 회색 배경 pill).
- 카드는 **한 과정의 마무리** 단위다. 여러 개가 될 수 있고, 최신이 위로 쌓인다.

**보조 카드 (개인 프로젝트)**
- 2열 그리드. 미디어 영역 16:9, 높이 약 100px. 개수 제한 없음 — 행이 늘어난다.
- 제목(15px/500), 메타(`개인 · 기간`), 본문 2~3행.
- 하단에 `readme ↗` 링크 — GitHub README로 나간다.
- 스택 태그 없음(정보량 억제).

**카드당 하나만 증명한다.** 세 카드가 각각 다른 것을 증명해야 방문자가 머릿속에서 인물을 합성한다.

---

## 3. 타이포그래피

| 용도 | 크기 | 서체 |
|---|---|---|
| 선언 문장 | 21px | Title |
| 카드 제목(대표) | 16px | Title |
| 카드 제목(보조) | 15px | Title |
| 본문 | 14px / 13px | Regular |
| 메타·라벨·태그·링크 | 11–12px | POP |

- ONE Mobile 3종(Regular / Title / POP)을 서브셋 WOFF2로 사용한다. 웹 임베딩 허용 확인됨.
- **Title과 POP은 단일 굵기다.** 500을 지정하면 가짜 볼드가 합성되므로 항상 `400`으로 쓴다.
  굵기가 아니라 **서체와 크기**로 위계를 만든다.
- 본문 `line-height: 1.6–1.7`. 본문 폭은 75자를 넘기지 않는다.
- POP은 메타데이터 전용. 제목과 본문에는 쓰지 않는다.

---

## 4. 색

**밝은 배경.** 다크 테마를 쓰지 않는다 — 게임 스크린샷이 대체로 어두워서
어두운 배경에 올리면 경계가 사라지고 전부 회색으로 뭉개진다. 밝은 배경이 액자 역할을 한다.

- 기본: 흰 배경, 거의 검은 본문 텍스트, 회색 3단계(본문/보조/힌트).
- 테두리는 `0.5px`. 얇게.
- 액센트 색은 **단 하나**, 쓰는 곳도 두 군데뿐: 대표 카드 테두리, 외부 링크.
- 카드 모서리 `8px`. 컨트롤 `8px`.

---

## 5. 미디어

- 프로필: 3:4, JPEG.
- 프로젝트: **16:9, 짧은 무음 루프**(6~10초), `<video muted loop playsinline preload="none">`.
- 반드시 `poster` 이미지를 지정한다. 로드 전/실패 시 빈 사각형이 남지 않게.
- 자동 재생을 데스크톱에서 하지 않는다. 3개가 동시에 로딩되면 느려진다.
- 해상도는 720p 이하, 파일당 2MB 이하를 목표로 한다.

### hover 재생과 모바일

**hover는 터치 기기에 존재하지 않는다.** 채용 담당자가 링크를 폰으로 먼저 여는 경우가 많다.
따라서 처음부터 두 경로를 모두 구현한다.

- 데스크톱(`@media (hover: hover)`): hover 시 재생, 벗어나면 일시정지.
- 터치(`@media (hover: none)`): 대표 카드 1개만 자동 재생. 보조 카드는 poster 정지 이미지 유지.
- 재생 여부와 무관하게 카드 전체가 링크로 동작해야 한다.

---

## 6. 모션

**모션은 콘텐츠의 공간 관계를 설명할 때만 쓴다.** 그 외에는 쓰지 않는다.

허용: hover 시 테두리 색 전환, 링크 밑줄, 영상 재생 전환. 모두 150–200ms 이내.

금지: 스크롤 연동 애니메이션, parallax, 페이드인 등장, 페이지 전환 효과.

---

## 7. 반응형

- 컨테이너 최대 폭 720–760px. 중앙 정렬.
- 좌우 여백은 모바일에서 최소 20px.
- 히어로: 좁은 화면에서 사진이 위, 텍스트가 아래로 세로 적층. 사진 폭은 절반 이하로.
- 보조 카드 2열 → 1열.
- 최소 지원 폭 320px.

---

## 8. 금지 목록

장효제를 설명하는 데 기여하지 않는 것은 전부 버린다.

- Three.js, WebGL, 커스텀 셰이더, 3D 씬
- 커스텀 커서, 로딩 시퀀스, 스플래시 화면
- glassmorphism, 네온 글로우, 그라디언트, 드롭섀도우
- 다크 배경, 터미널 에뮬레이터 연출, 타이핑 애니메이션
- 배경 이미지 — 어두운 게임 영상의 액자 역할을 하는 흰 배경을 무너뜨린다
- **기술 스택 로고 그리드** — 정보량이 0이다. Unreal 로고가 붙어 있다고 얼마나 아는지는 드러나지 않는다.
- 스킬 레벨 게이지, 별점, 퍼센트 바
- 방문자 카운터, 애널리틱스 배지, "made with ♥"
- AI 개발자 포트폴리오 템플릿의 관습적 요소 일반

---

## 9. 접근성 최소선

- `<video>`에 자동 재생이 걸린 경우 `prefers-reduced-motion` 존중.
- 이미지 `alt` 필수. 장식용 요소는 `aria-hidden`.
- 텍스트 대비 4.5:1 이상. 가장 흐린 회색도 이 기준을 넘겨야 한다.
- 키보드 탭 순서가 시각적 순서와 일치. 포커스 링을 제거하지 않는다.
- 카드가 링크라면 `<a>`로 감싼다. `onclick`으로 대체하지 않는다.

---

## 10. 콘텐츠

문구를 창작하지 않는다. 근거와 자리표시자 규칙은 `AGENTS.md`.

---

## 11. 완료 기준

- `index.html` + `style.css` 두 파일로 끝난다.
- 빌드 명령 없이 브라우저에서 `index.html`을 열어 확인 가능하다.
- 360px 폭과 1440px 폭에서 모두 레이아웃이 무너지지 않는다.
- 미디어 파일이 전부 없는 상태에서도 poster 자리가 회색 사각형으로 유지되며 레이아웃이 깨지지 않는다.
- 위 금지 목록의 항목이 하나도 포함되지 않는다.
